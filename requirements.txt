# Main env: data processing + training (LLaMA-Factory / SFT / DPO).
# For vLLM inference see requirements-vllm.txt (install PyTorch 2.4 + CUDA 12.1 first).
torch==2.6.0
torchvision==0.21.0
transformers==4.50.0
datasets==3.2.0
accelerate==1.2.1
peft==0.15.1
trl==0.9.6